# Ubicación: /conexapi/conexapi_backend/.env.example
# Propósito: Este archivo es una plantilla para las variables de entorno.
#            Muestra qué variables necesita la aplicación, pero NO contiene valores reales.
#            Los valores reales van en el archivo .env (que Git ignora por seguridad).

# Cadena de conexión a la base de datos PostgreSQL.
# Formato: postgresql+psycopg2://USUARIO:CONTRASEÑA@HOST:PUERTO/NOMBRE_DB
# Ejemplo: DATABASE_URL="postgresql+psycopg2://mi_usuario:mi_password@mi_host.neon.tech:5432/conexapi_prod"
DATABASE_URL=""

# Clave secreta para firmar los tokens JWT. GENERAR UNA CLAVE LARGA Y ALEATORIA.
# Puedes generar una con: openssl rand -hex 32 (en Linux/macOS) o busca un generador online.
SECRET_KEY=""